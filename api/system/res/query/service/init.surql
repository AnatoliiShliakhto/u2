BEGIN TRANSACTION;

DEFINE NAMESPACE IF NOT EXISTS $namespace;
USE NS $namespace;

DEFINE DATABASE IF NOT EXISTS $database;
USE DB $database;

DEFINE USER IF NOT EXISTS $user ON DATABASE PASSWORD '$password' ROLES OWNER;

RETURN true;

COMMIT TRANSACTION;